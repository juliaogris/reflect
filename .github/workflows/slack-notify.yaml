name: slack-notify
on:
  status:
  check_run:
    types: [created,completed]
  check_suite:
    types: [created,completed]
jobs:
  slack-notify:
    runs-on: ubuntu-latest
    steps:
    - run: echo ${{ github.event_name }}
